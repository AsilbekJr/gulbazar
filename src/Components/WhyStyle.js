import { Grid } from "@mui/material";
import { styled } from "@mui/material/styles";


export const whyGrid = styled(Grid)(({theme}) => ({
    // padding:"1.5rem",
    // textAlign:"center",
    // display:"flex",
    // alignItems:"center",
    // flexDirection:"column"
}))